// Modules

const { names } = require("./4 - names");
const { names_too } = require("./4 - names");
const sayHi = require("./5 - utils");
require ("./7 - mindgrenade")

/*names.map((name) => {
  sayHi(name);
});

names_too.map((name) => {
  sayHi(name);
});*/

